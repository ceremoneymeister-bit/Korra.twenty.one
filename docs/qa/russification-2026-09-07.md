# Русификация Korra 21 — приёмка wave3

Дата завершения: 08.09.2026. База волны: `20cf8035d93eadc8f50e549e0b5d5ce47ea4b645`.
Принятый код русификации перед этим отчётом: `ffc2ca70fcfd0f665e950ae88c140184f3212b4a`.

## Результат

Человеко-видимые ответы Korra переведены на русский в шлюзе, Telegram, Discord,
Slack, CLI, настройке провайдеров, диагностике, обновлении, резервном копировании,
памяти, проектах, секретах, инструментах и странице входа панели.

- В диапазоне wave3: 60 коммитов, из них 57 относятся к русификации и 3 — к
  соседнему центру обновлений.
- Диапазон меняет 511 файлов, включая 348 тестовых файлов.
- Финальный инвентарь содержит 11 169 проверенных пользовательских строк и
  шаблонов; ещё 1 001 запись найдена заключительным остаточным срезом.
- Инвентарь и воспроизводимые артефакты находятся в
  `/root/Antigravity/projects/Korra 21/agents-work/wave3-20260907/`.

## Границы совместимости

Сохранены без перевода:

- команды, аргументы CLI, callback/API keys, persisted identifiers и статусы;
- машинные маркеры, regex-классификаторы, заголовки протоколов и receipts;
- logger-сообщения и сырой stderr внешних программ, необходимые диагностике;
- инструкции для модели, системные prompts и управляющие маркеры наподобие
  `LOOP_COMPLETE` и `_INTERRUPT_REASON_*`;
- официальные названия элементов сторонних интерфейсов, когда по ним пользователь
  должен найти настройку.

Причины опасных команд переводятся только в display-копии. Канонические причины,
ключи разрешений и очередь одобрений сохраняют исходные машинные значения.

## Проверки

Все Python-тесты запускались штатным `scripts/run_tests.sh` с чистым `HOME` и
Python 3.11 из `.venv`.

1. Основные тематические срезы до финализации:
   - gateway/setup: 578 passed, 0 failed, 10 skipped;
   - update/recovery: 712 passed, 0 failed, 5 skipped;
   - provider/runtime: 717 passed, 0 failed, 4 skipped;
   - interactive CLI: 1 427 passed, 0 failed, 8 skipped;
   - live API JSON/SSE: русское безопасное объяснение ошибки провайдера;
   - browser smoke: карточка одобрения, одноразовое разрешение и ответ агента,
     browser errors отсутствуют.
2. Финальный полный репозиторный прогон обнаружил 66 падений в 39 файлах. Он шёл
   из общей рабочей директории и включал незакоммиченный updater другого трека и
   untracked-артефакты: 3 513 файлов, 42 643 passed, 66 failed, 355 skipped.
3. Повтор тех же 39 файлов из чистого worktree на `HEAD` отделил посторонний
   dirty-state: 1 265 passed, 54 failed, 1 skipped в 36 файлах.
4. После обновления только эквивалентных русских ожиданий и восстановления
   Windows-scan аннотации единый повтор всех 36 файлов прошёл: **1 213 passed,
   0 failed, 1 Windows-only skipped**.
5. Полный dashboard-auth набор после path-prefix, same-origin и ARIA-правок:
   **203 passed, 0 failed**. Отдельный adversarial review нашёл unsafe-prefix и
   double-prefix случаи; regression-тесты добавлены, повторный verdict — `GO`.
6. `py_compile`, import-smoke, `git diff --check` и Windows footgun scan прошли.

Доказательства:

- `ru-final-full-suite.txt` — полный диагностический прогон;
- `ru-final-failures-clean.txt` — изоляция падений в чистом worktree;
- `ru-final-regressions-green.txt` — итоговые 1 213/0;
- `ru-cli-help-final.txt`, `ru-cli-status-final.txt`, `ru-api-final.txt`;
- `ru-browser.txt`, `ru-approval.png`, `ru-chat-result.png`;
- `ru-inventory.md` и JSON-инвентари по слоям.

## Закрытие замечаний релизного review

Второй Sol-review остановил сборку на двух оставшихся пользовательских ветках.
Они закрыты отдельными bounded-коммитами:

- `56e0951b5e` — русский вывод `korra peer`, включая ошибки, состояния и
  подсказки с командой `korra peer`; 24 теста;
- `473366892f` — русский вывод `korra mcp add/remove/test/login/reauth/configure`
  и интерактивных picker/OAuth-веток; 46 тестов.

Совместный прогон двух файлов: **70 passed, 0 failed**. Прямые smoke `peer list`,
`mcp list`, `mcp add` без транспорта и `mcp login` для отсутствующего сервера
показывают русский текст. Машинные API/JSON/config/OAuth/PKCE/tool/status/choice
значения сохранены. Свежий независимый review четырёх файлов дал `GO`.

Финальный проверенный код перед сборкой: `473366892f2f23f288eb9a0c52854cd31fa25a2e`.
Tracked working tree чист; `agents-work/` содержит только незакоммиченные рабочие
артефакты и не входит в образ.

## Не входит в завершение русификации

Fix освобождения drain и его regression/E2E уже закрыты отдельным треком центра
обновлений (`09f046c850` и `docs/qa/update-center-ui-2026-09-07.md`).

Релизный Sol-review всей wave3, сборка образа, публикация и rollout выполняются
следующими этапами оркестратора. Во время русификации живые контуры, registry и
клиентские серверы не изменялись.
